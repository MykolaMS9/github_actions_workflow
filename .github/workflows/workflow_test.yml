name: Bats Tests (DinD)

on:
  push:
    branches: [master, alpha]
  pull_request:
    branches: [master, alpha]

jobs:
  test:
    runs-on: [self-hosted, hetzner]

    container:
      image: docker:24-cli
      env:
        DOCKER_TLS_CERTDIR: ""
      options: >-
        -v /opt/ci:/opt/ci
        -v dind-run:/var/run
        -v dind-storage:/var/lib/docker

    services:
      docker:
        image: docker:24-dind
        env:
          DOCKER_TLS_CERTDIR: ""
        options: >-
          --privileged
          -v dind-run:/var/run
          -v dind-storage:/var/lib/docker

    steps:
      - uses: actions/checkout@v4

      - name: Build helper image (CLI + bats)
        run: docker build -t ci-docker-bats -f /opt/ci/dood-helper.Dockerfile /opt/ci

      - name: Build app image inside DinD
        run: docker build -t app-tests -f /opt/ci/Dockerfile .

      - name: Run Bats tests inside DinD
        run: |
          docker run --rm \
            -v "$PWD":/workspace \
            -w /workspace \
            app-tests bash -lc "
              command -v dos2unix >/dev/null 2>&1 && \
                find tests -name '*.bats' -exec dos2unix {} \; || true
              find tests -name '*.bats' -exec bats {} +
            "
